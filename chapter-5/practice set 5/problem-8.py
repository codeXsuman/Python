# 9. Can you change the values inside a list which is contained in set S? s = {8, 7, 12, "hello", [1, 2, 3]}

s = {8, 7, 12, "hello", [1, 2, 3]}

# answer: No, because lists are mutable and sets require their elements to be immutable (hashable).

# we can not included a list indide a set.